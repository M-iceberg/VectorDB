float:
  any:
    args:
      - "@metric"
    module: "ann_benchmarks.algorithms.vectordb.module"
    name: "VectorDB"
    query-args:
      - [50, 100, 200, 400, 800]
    base-args:
      - {"M": 16, "efConstruction": 200}
